services:
    app:
        build:
            context: .
            dockerfile: Dockerfile
        volumes:
            - ..:/workspace:cached
        command: sleep infinity
    nginx:
        image: nginx
    echoserver:
        image: ealen/echo-server
        environment:
            - "PORT=5000"