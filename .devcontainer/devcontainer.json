{
  "name": "HTTP with devcontainer and docker compose Sandbox",
  "service": "app",
  "dockerComposeFile": "docker-compose.yml",
  "workspaceFolder": "/workspace",
  "forwardPorts": ["echoserver:5000"]
}
